apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uyuni-front-end.fullname" . }}-test-connection"
  labels:
    {{- include "uyuni-front-end.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uyuni-front-end.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
